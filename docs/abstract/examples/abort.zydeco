def rec abort : Thunk(
  Int -> Int *-> Ret(Int)
) = {
  comatch
  | .arg(x) ->
    comatch
    | .more(_) -> (! abort).arg(x)
    | .done( ) -> ret x
};

(! abort).arg(0)
  .more(1).more(2).more(3)
  .done( )
