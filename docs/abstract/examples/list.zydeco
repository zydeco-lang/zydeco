data List A where
  | Nil( )
  | Cons(A, List A)

def rec foldl : Thunk(
  Thunk(A -> A' -> Ret(A')) ->
  List A -> A' -> Ret(A')
) = {
  fn (step, l, a') ->
    match l
    | Nil( ) -> ret a'
    | Cons(hd, tl) -> (
      do a' <- ! step hd a';
      ! foldl tl step a' )
};