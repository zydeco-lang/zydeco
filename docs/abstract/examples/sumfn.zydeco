def rec sum : U(List Int -> F(Int)) = {
  fn (xs: List Int) ->
    match xs
    | Done() -> ret 0
    | More (x, xs) ->
      ( do y <- ! sum xs;
        ! add x y )
};

! sum (More (1, More (2, Done())))