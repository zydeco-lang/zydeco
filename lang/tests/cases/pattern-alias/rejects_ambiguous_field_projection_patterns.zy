-- expect: reject(tyck.duplicate-named-field)

begin
  let Ambiguous = (#left :: #x :: Int64) * (#right :: #x :: Int64) that
  def ambiguous : Ambiguous = (#left = #x = 1, #right = #x = 2) that
  let (/x = duplicate) = ambiguous in
  duplicate
end
