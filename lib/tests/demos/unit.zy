-- Unit defined in std.zydeco
begin
  param (/Ret; /Unit; /stdio; /process; /String; /system) : @(import("../../std/builtin.zy")) that
  def ! unit_to_str (_ : Unit) : Ret String = ret "()" that

  let v : Unit = () in
  do x <- ! unit_to_str v;
  ! stdio/write_line x { ! process/exit 0 }
end
