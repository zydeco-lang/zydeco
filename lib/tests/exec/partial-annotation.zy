begin
  param (
    (
      /VType;
      /CType;
      /Thk;
      /OS;
      /exit
    ) :
    @[import("../../std/builtin.zy")] _
  ) that

  def Kont (A : VType) (B : CType) =
    data
    | +Running : Thk B
    | +Done : A
    end
  that

  let k : Kont _ OS = +Done(1) in
  ! (exit) 0
end
