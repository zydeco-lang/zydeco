begin
  param (
    (/VType; /CType; /Thk; /OS; /process) :
    @[import("../../std/builtin.zy")] _
  ) that

  def Kont (A : VType) (B : CType) =
    data
    | +Running : Thk B
    | +Done : A
    end
  that

  let k : Kont _ OS = +Done(1) in
  ! (process/exit) 0
end
