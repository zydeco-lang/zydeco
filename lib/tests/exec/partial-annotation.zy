begin
  param (
    (VType, CType, Thk, Ret, Unit, Int, Char, String, OS, api) :
    @[import("../../std/builtin.zy")] _
  ) that

  let Kont (A : VType) (B : CType) =
    data
    | +Running : Thk B
    | +Done : A
    end
  that

  let k : Kont _ OS = +Done(1) in
  ! (api/exit) 0
end
