begin
  param (/OS; /process; /Int64; /system) : @(import("../../std/builtin.zy")) that
  def Choice =
    codata
    | .left : OS
    | .right : Int64 -> OS
    end
  that

  (
    comatch
    | .left => ! process/exit 10
    | .right i => ! process/exit i
    end : Choice
  ) .right 0
end
