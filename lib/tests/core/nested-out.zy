param (/process; /Int; /numeric; /system) : @(import("../../std/builtin.zy")) in
let int = numeric/int in
do x <- ret 40;
do y <- ret 30;
do z <-
  do a <- ! int/add x y;
  do b <- ! int/add a 10;
  ret x;
do r <- ! int/sub z 40;
! process/exit r
