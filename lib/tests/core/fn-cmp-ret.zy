begin
  param (/Ret; /process; /Int; /numeric; /system) : @(import("../../std/builtin.zy")) that
  let int = numeric/int that
  def ! f (x : Int) : Ret Int = ! int/eq
    (Ret Int)
    x
    0
    { ret 0 }
    {
      ret 1
    } that

  do z <- ! f 0;
  ! process/exit z
end
