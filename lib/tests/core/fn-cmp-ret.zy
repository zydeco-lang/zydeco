begin
  param (/Ret; /process; /Int64; /numeric; /system) : @(import("../../std/builtin.zy")) that
  let int64 = numeric/int64 that
  def ! f (x : Int64) : Ret Int64 = ! int64/eq
    (Ret Int64)
    x
    0
    { ret 0 }
    {
      ret 1
    } that

  do z <- ! f 0;
  ! process/exit z
end
