begin
  param (/Unit; /process; /system) : @(import("../../std/builtin.zy")) that
  def Bool =
    data
    | +True : Unit
    | +False : Unit
    end
  that

  match +True()
  | +True() => ! process/exit 0
  | +False() => ! process/exit 1
  end
end
