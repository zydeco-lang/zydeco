begin
  param (/VType; /Ret; /process; /Int64; /system) : @(import("../../std/builtin.zy")) that
  def ! id (A : VType) (a : A) : Ret A = ret a that

  do x <- ! id Int64 0;
  do y <- ! id Int64 x;
  do z <- ! id Int64 y;
  do r <- ! id Int64 z;
  ! process/exit r
end
