begin
  let make_std = @(import("../../std/std.zy")) that
  param (/VType; /CType; /Thk; /Ret; /Int64; builtin) : @(import("../../std/builtin.zy")) that

  let (/int64; /process) = builtin |> make_std in
  do one <- ! int64/increment 0;
  do status <- ! int64/sub one 1;
  ! process/exit status
end
