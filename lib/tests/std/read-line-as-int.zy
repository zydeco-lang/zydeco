begin
  let std = @[import("../../std/std.zy")] _ that
  param (
    (VType, CType, Thk, Ret, Unit, Int, Char, String, OS, api) :
    @[import("../../std/builtin.zy")] _
  ) that

  do (
    Bool = StdBool,
    Option = StdOption,
    List = StdList,
    Int = StdInt,
    Char = StdChar,
    String = StdString,
    OS = StdOS,
    public
  ) <- ! std (VType, CType, Thk, Ret, Unit, Int, Char, String, OS, api);
  ! (public/read_line_as_int) {
    fn first ->
      ! (public/option_eliminate)
        StdInt
        StdOS
        first
        { ! (public/exit) 1 }
        {
          fn integer ->
            ! (public/read_line_as_int) {
              fn second ->
                ! (public/option_eliminate)
                  StdInt
                  StdOS
                  second
                  { ! (public/exit) 0 }
                  {
                    fn unexpected ->
                      ! (public/exit) 1
                  }
            }
        }
  }
end
