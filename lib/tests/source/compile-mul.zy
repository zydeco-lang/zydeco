param (
  (VType, CType, Thk, Ret, Unit, Int, Char, String, OS, api) :
  @[import("../../std/builtin.zy")] _
) in
  do product <- ! (api/mul) 10 10;
  do status <- ! (api/sub) product 100;
  ! (api/exit) status
