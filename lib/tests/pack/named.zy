begin
  param (
    (/VType; /Thk; /Ret; /Int; /add; /sub; /exit) :
    @[import("../../std/builtin.zy")] _
  ) that
  let CounterLibrary =
    exists (
      Counter = Representation as Int :
      VType
    ) .
      (zero :: Representation) *
      (increment :: Thk (Representation -> Ret Representation))
  that
  def counter : CounterLibrary = (
    Counter = Int,
    zero = 0,
    increment = {
      fn (value : Int) =>
        ! (add) value 1
    },
  ) that
  def ! use_counter ((= Counter, = zero, = increment) : CounterLibrary) : Ret Int =
    ! increment zero
  that

  do value <- ! use_counter counter;
  do status <- ! (sub) value 1;
  ! (exit) status
end
