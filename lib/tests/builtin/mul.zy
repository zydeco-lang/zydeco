param (/numeric; /system) : @(import("../../std/builtin.zy")) in
let (= Int64, int64) = numeric/int64 in
let (/process) = system in
do z <- ! int64/mul 10 10;
do r <- ! int64/sub z 100;
! process/exit r
