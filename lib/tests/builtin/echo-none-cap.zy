begin
  param (/OS; /stdio; /process; /Int; /system) : @(import("../../std/builtin.zy")) that
  ! stdio/read_line {
    fn line =>
      let ! f (z : Int) : OS =
        let line = line in
        ! process/exit z
      in
      ! f 0
  }
end
