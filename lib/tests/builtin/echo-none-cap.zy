begin
  param (/OS; /stdio; /process; /Int64; /system) : @(import("../../std/builtin.zy")) that
  ! stdio/read_line {
    fn line =>
      let ! f (z : Int64) : OS =
        let line = line in
        ! process/exit z
      in
      ! f 0
  }
end
