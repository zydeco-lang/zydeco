begin
  param (
    (VType, CType, Thk, Ret, Unit, Int, Char, String, OS, api) :
    @[import("../../std/builtin.zy")] _
  ) that
  def ! id (A : VType) (a : A) : Ret A =
    ret a
  that

  do x <- ! id Int 0;
  ! (api/exit) x
end
