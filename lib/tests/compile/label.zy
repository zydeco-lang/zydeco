begin
  param (
    (VType, CType, Thk, Ret, Unit, Int, Char, String, OS, api) :
    @[import("../../std/builtin.zy")] _
  ) that
  def identity : Thk (
    forall (A : VType) .
      A -> Ret A
  ) = {
    fn (A : VType) (value : A) ->
      ret value
  } that

  do status <- ! identity Int 0;
  ! (api/exit) status
end
