param (
  (
    /add;
    /sub;
    /exit
  ) :
  @[import("../../std/builtin.zy")] _
) in
  do x <- ret 40;
  do y <- ret 30;
  do z <-
    do a <- ! (add) x y;
    do b <- ! (add) a 10;
    ret x;
  do r <- ! (sub) z 40;
  ! (exit) r
