begin
  param (
    (/Int; /OS; /exit) :
    @[import("../../std/builtin.zy")] _
  ) that
  def Choice =
    codata
    | .left : OS
    | .right : Int -> OS
    end
  that

  (comatch
  | .left => ! (exit) 10
  | .right i => ! (exit) i
  end : Choice) .right 0
end
