begin
  param (
    (/Int; /OS; /process) :
    @[import("../../std/builtin.zy")] _
  ) that
  def Choice =
    codata
    | .left : OS
    | .right : Int -> OS
    end
  that

  (comatch
  | .left => ! (process/exit) 10
  | .right i => ! (process/exit) i
  end : Choice) .right 0
end
