begin
  def Bool =
    data
    | +False : Unit
    | +True : Unit
    end
  that
  param (
    (/Unit; /process) :
    @[import("../../std/builtin.zy")] _
  ) that

  let x : Bool = +False() in
  match x
  | +True(_) => ! (process/exit) 10
  | +False(_) => ! (process/exit) 0
  end
end
