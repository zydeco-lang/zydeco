begin
  def Bool =
    data
    | +False : Unit
    | +True : Unit
    end
  that
  param (
    (/Unit; /exit) :
    @[import("../../std/builtin.zy")] _
  ) that

  let x : Bool = +False() in
  match x
  | +True(_) => ! (exit) 10
  | +False(_) => ! (exit) 0
  end
end
