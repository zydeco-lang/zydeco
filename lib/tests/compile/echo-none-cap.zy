begin
  param (
    (/Int; /OS; /stdio; /process) :
    @[import("../../std/builtin.zy")] _
  ) that

  ! (stdio/read_line) {
    fn line =>
      let ! f (z : Int) : OS =
        let line = line in
        ! (process/exit) z
      in
      ! f 0
  }
end
