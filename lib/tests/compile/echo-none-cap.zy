begin
  param (
    (
      /Int;
      /OS;
      /read_line;
      /exit
    ) :
    @[import("../../std/builtin.zy")] _
  ) that

  ! (read_line) {
    fn line =>
      let ! f (z : Int) : OS =
        let line = line in
        ! (exit) z
      in
      ! f 0
  }
end
