begin
  param (
    (/add; /sub; /exit) :
    @[import("../../std/builtin.zy")] _
  ) that

  do x <- ret 40;
  do y <- ret 30;
  do z <-
    do a <- ! (add) x y;
    do b <- ! (add) a 10;
    ret b;
  do r <- ! (sub) z 80;
  ! (exit) r
end
