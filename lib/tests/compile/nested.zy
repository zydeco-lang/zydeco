begin
  param (
    (/int; /process) :
    @[import("../../std/builtin.zy")] _
  ) that

  do x <- ret 40;
  do y <- ret 30;
  do z <-
    do a <- ! (int/add) x y;
    do b <- ! (int/add) a 10;
    ret b;
  do r <- ! (int/sub) z 80;
  ! (process/exit) r
end
