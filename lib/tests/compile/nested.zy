begin
  param (
    (VType, CType, Thk, Ret, Unit, Int, Char, String, OS, api) :
    @[import("../../std/builtin.zy")] _
  ) that

  do x <- ret 40;
  do y <- ret 30;
  do z <-
    do a <- ! (api/add) x y;
    do b <- ! (api/add) a 10;
    ret b;
  do r <- ! (api/sub) z 80;
  ! (api/exit) r
end
