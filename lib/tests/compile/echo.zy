begin
  param (
    (/core; /system) :
    @[import("../../std/builtin.zy")] _
  ) that
  let (/Thk) = core that
  let (/OS; /stdio) = system that

  fix (loop : Thk OS) =>
    ! (stdio/read_line) {
      fn line =>
        ! (stdio/write_line) line loop
    }
end
