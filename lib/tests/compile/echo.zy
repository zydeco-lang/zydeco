begin
  param (
    (
      /Thk;
      /OS;
      /write_line;
      /read_line
    ) :
    @[import("../../std/builtin.zy")] _
  ) that

  fix (loop : Thk OS) =>
    ! (read_line) {
      fn line =>
        ! (write_line) line loop
    }
end
