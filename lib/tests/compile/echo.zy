begin
  param (
    (/Thk; /OS; /stdio) :
    @[import("../../std/builtin.zy")] _
  ) that

  fix (loop : Thk OS) =>
    ! (stdio/read_line) {
      fn line =>
        ! (stdio/write_line) line loop
    }
end
