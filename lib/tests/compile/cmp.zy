param (
  (/numeric; /system) :
  @[import("../../std/builtin.zy")] _
) in
let (Scalar = Int64, int64) = numeric/int64 in
let (/OS; /process) = system in
  ! (int64/eq)
    OS
    10
    10
    { ! (process/exit) 0 }
    { ! (process/exit) 1 }
