param (
  (/numeric; /system) :
  @(import("../../std/builtin.zy"))
) in
let (#Scalar = Int64, int64) = numeric/int64 in
let (/process) = system in
do x <- ret 40;
do y <- ret -40;
do z <- ! (int64/add) x y;
! (process/exit) z
