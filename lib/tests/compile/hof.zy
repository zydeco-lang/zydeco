begin
  param (
    (/VType; /CType; /Thk; /Ret; /Int; /exit) :
    @[import("../../std/builtin.zy")] _
  ) that
  def ! apply (A : VType) (B : CType) (f : Thk (A -> B)) (x : A) : B =
    ! f x
  that

  do x <- ! apply Int (Ret Int) { fn x => ret x } 0;
  ! (exit) x
end
