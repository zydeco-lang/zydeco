begin
  param (
    (/Ret; /Int; /int_eq_branch; /exit) :
    @[import("../../std/builtin.zy")] _
  ) that
  def ! f (x : Int) : Ret Int =
    ! (int_eq_branch)
      (Ret Int)
      x
      0
      { ret 0 }
      { ret 1 }
  that

  do z <- ! f 0;
  ! (exit) z
end
