begin
  param (
    (/Ret; /Int; /int; /process) :
    @[import("../../std/builtin.zy")] _
  ) that
  def ! f (x : Int) : Ret Int =
    ! (int/eq)
      (Ret Int)
      x
      0
      { ret 0 }
      { ret 1 }
  that

  do z <- ! f 0;
  ! (process/exit) z
end
