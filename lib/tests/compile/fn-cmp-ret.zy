begin
  param (
    (/Ret; /Int64; /int64; /process) :
    @[import("../../std/builtin.zy")] _
  ) that
  def ! f (x : Int64) : Ret Int64 =
    ! (int64/eq)
      (Ret Int64)
      x
      0
      { ret 0 }
      { ret 1 }
  that

  do z <- ! f 0;
  ! (process/exit) z
end
