pub extern def read_line : Thk (Thk (String -> OS) -> OS) end
pub extern def write_line : Thunk(String -> Thk OS -> OS) end
pub extern def exit : Thunk(Int -> OS) end

main
  ! read_line {
    fn (line: String) ->
    ! write_line line { ! exit 0 }
  }
end
