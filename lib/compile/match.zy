extern def exit : Thk (Int -> OS) end

alias Bool : VType =
  data
  | +False : Unit
  | +True : Unit
  end
end

main
  let x: Bool = +False () in
  match x
  | +True _ -> ! exit 10
  | +False _ -> ! exit 0
  end
end
