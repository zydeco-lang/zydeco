begin
  let TextBody = @(import("body.type.zy")) in
  let DataPackage = @(import("../data/package.type.zy")) in
  pi (_ : @(import("../builtin.zy"))) .
    pi
      (
        (/Bool; /Option; /List) :
        DataPackage
      )
    .
      TextBody Bool Option List
end
