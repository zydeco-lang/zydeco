begin
  let DataPackage = @(import("../data/package.type.zy")) in
  let TextPackage = @(import("package.type.zy")) in
  pi (_ : @(import("../builtin.zy"))) .
    pi
      (
        (/Bool; /Option; /List) :
        DataPackage
      )
    .
      TextPackage Bool Option List
end
