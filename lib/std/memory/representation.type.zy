begin
  let VType = @(intrinsic(vtype)) in
  param Storage : VType -> VType in
  param Codec : VType -> VType -> VType in
  fn (A : VType) => exists (= L : VType) . (#storage :: Storage L) * (#codec :: Codec L A)
end
