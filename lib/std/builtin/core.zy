exists (VType as @(intrinsic(vtype)))
  (CType as @(intrinsic(ctype)))
  (
    #Thk = CoreThk as @(intrinsic(thk)) :
    CType -> VType
  )
  (
    #Ret = CoreRet as @(intrinsic(ret)) :
    VType -> CType
  )
  (#Unit = CoreUnit as @(intrinsic(unit)) : VType)
.
  @(intrinsic(unit))
