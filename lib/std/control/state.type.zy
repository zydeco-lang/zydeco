begin
  let VType = @(intrinsic(vtype)) in
  let CType = @(intrinsic(ctype)) in
  let Unit = @(intrinsic(unit)) in
  let MonadState
    (S : VType) (M : VType -> CType)
  =
    codata
    | .get : M S
    | .put : S -> M Unit
    end
  in
  MonadState
end
