begin
  let Std = @[import("std.type.zy")] _ in
  pi
    (
      (
        /core;
        /representations;
        /numeric;
        /system = (/Reader; /Writer; /OS);
        builtin
      )
      : @[import("builtin.zy")] _
    )
  .
    Std Reader Writer OS
end
