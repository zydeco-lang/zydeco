begin
  let Std = @(import("std.type.zy")) in
  pi
    (
      (
        /core;
        /representations;
        /numeric;
        /system = (/Reader; /Writer; /OS);
        builtin
      )
      : @(import("builtin.zy"))
    )
  .
    Std Reader Writer OS
end
