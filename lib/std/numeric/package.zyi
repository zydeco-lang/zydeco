begin
  let DataPackage = @(import("../data/package.type.zy")) in
  let NumericBody = @(import("body.type.zy")) in
  pi
    (
      (/core; /representations; /numeric) :
      @(import("../builtin.zy"))
    )
  .
    pi
      (
        (/Bool; /bool) :
        DataPackage
      )
    .
      NumericBody Bool
end
