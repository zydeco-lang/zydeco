begin
  let VType = @(intrinsic(vtype)) in
  let Unit = @(intrinsic(unit)) in
  let Numeric = @(import("../numeric.type.zy")) in
  let Float64 = @(intrinsic(f64)) in
  let Float64Instance (Bool : VType) =
    exists (#Float64 = ScalarType as Float64 : VType) .
      (#dictionary :: Numeric Bool ScalarType) * Unit
  in
  Float64Instance
end
