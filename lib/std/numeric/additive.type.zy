begin
  let VType = @(intrinsic(vtype)) in
  let Thk = @(intrinsic(thk)) in
  let Ret = @(intrinsic(ret)) in
  param A : VType in
    (#zero :: A)
  * (#add :: Thk (A -> A -> Ret A))
  * (#sub :: Thk (A -> A -> Ret A))
  * (#negate :: Thk (A -> Ret A))
end
