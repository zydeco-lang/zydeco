begin
  let VType = @(import("../builtin/intrinsic/vtype.zy")) in
  let Thk = @(import("../builtin/intrinsic/thk.zy")) in
  let Ret = @(import("../builtin/intrinsic/ret.zy")) in
  let PartialEquality (Bool : VType)
    (A : VType) =
      (eq :: Thk (A -> A -> Ret Bool))
    * (ne :: Thk (A -> A -> Ret Bool))
  in
  PartialEquality
end
