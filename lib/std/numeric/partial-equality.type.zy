begin
  let VType = @(intrinsic(vtype)) in
  let Thk = @(intrinsic(thk)) in
  let Ret = @(intrinsic(ret)) in
  param Bool : VType in
  param A : VType in
    (#eq :: Thk (A -> A -> Ret Bool))
  * (#ne :: Thk (A -> A -> Ret Bool))
end
