{
  "name": "cajun-zydeco-language-server",
  "displayName": "Zydeco (Cajun)",
  "description": "Zydeco language support powered by the Cajun language server",
  "license": "MIT",
  "version": "0.1.0",
  "keywords": [
    "Zydeco",
    "language-server",
    "LSP"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/zydeco-lang/zydeco"
  },
  "publisher": "LitiaEeloo",
  "categories": [
    "Programming Languages"
  ],
  "engines": {
    "vscode": "^1.96.0"
  },
  "activationEvents": [
    "onLanguage:zydeco"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "zydeco",
        "aliases": [
          "Zydeco",
          "zydeco"
        ],
        "extensions": [
          ".zy",
          ".zydeco"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "zydeco",
        "scopeName": "source.zydeco",
        "path": "./syntaxes/zydeco.tmLanguage.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Cajun",
      "properties": {
        "cajun.server.path": {
          "type": "string",
          "scope": "machine",
          "default": "",
          "description": "Path to the Cajun executable. Leave empty to find cajun on PATH."
        },
        "cajun.trace.server": {
          "type": "string",
          "scope": "window",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Trace communication with Cajun."
        },
        "cajun.format.lineWidth": {
          "type": "number",
          "scope": "window",
          "minimum": 1,
          "default": 100,
          "description": "Target line width in columns used by the formatter."
        },
        "cajun.format.layoutIntentions": {
          "type": "string",
          "scope": "window",
          "enum": [
            "preserve",
            "blank-lines-only",
            "ignore"
          ],
          "enumDescriptions": [
            "Preserve observed line breaks and blank lines.",
            "Preserve only blank lines; the line width decides every single break.",
            "Let the line width decide every optional break."
          ],
          "default": "preserve",
          "description": "Which parsed line-breaking choices the formatter retains."
        }
      }
    }
  },
  "scripts": {
    "bundle": "esbuild ./src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "check": "tsc --noEmit",
    "compile": "npm run check && npm run bundle -- --sourcemap",
    "watch": "npm run bundle -- --sourcemap --watch",
    "vscode:prepublish": "npm run check && npm run bundle -- --minify",
    "package": "vsce package --no-dependencies",
    "publish": "vsce publish --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "@types/vscode": "1.96.0",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "packageManager": "pnpm@11.9.0"
}
